pipeline {
    agent any

    stages {
        stage('Print Environment') {
            steps {
                echo 'Printing Environment Variables...'
                echo 'printenv'
            }
        }
    }
}
